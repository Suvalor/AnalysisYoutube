# 项目需求文档（PRD）— 出海导航模块全面重构升级

## 项目概述
对现有"出海导航"模块进行全面重构升级，解决推荐同质化问题，提升 AI 推荐深度和 UI 专业度。在已有功能基础上打磨表单、AI逻辑和UI三方面。

## 目标用户与使用场景
- **目标用户**：准备或正在 YouTube 海外市场做内容的创作者/小团队
- **使用场景**：用户输入自身资源画像，获得 AI 深度推荐的细分品类、行动路线图和避坑建议，并可直接跳转蓝海雷达验证

## 核心功能列表

### P0 — 必须完成
1. **表单增强**：新增3个输入字段
   - `existing_channel_url` (可选): 已有 YouTube 频道 URL，让 AI 分析现有内容
   - `target_regions` (多选): 目标市场地区（美国、东南亚、中东、欧洲等）
   - `weekly_hours` (下拉): 每周可投入时间（<5h / 5-10h / 10-20h / 20h+）
2. **AI 推荐逻辑深度优化**
   - 推荐数量从 2 个增加到 3 个（增加一个「中等匹配度但高增长潜力」品类）
   - 每个推荐增加 `action_roadmap` 字段：30天行动路线图（JSON数组，每项含 day_range + task + expected_result）
   - 每个推荐增加 `estimated_monthly_income` 字段：预估月收入范围（如 "$200-800"）
   - 支持多轮对话：用户可对推荐结果追问，AI 基于上下文继续分析
3. **UI 视觉升级（现代SaaS极简风）**
   - Notion/Linear 风格：大量留白 + 微妙动效 + 卡片阴影 + 精细排版
   - 推荐卡片重构：匹配度进度条替代 Tag、星级用真实图标、行动路线图用 Timeline 组件
   - 避坑卡片优化：更精致的警告色调
   - 表单优化：分组布局、步骤引导感

### P1 — 应该完成
4. **已有频道分析**：如果用户提供了频道 URL，调用 YouTube API 获取频道信息并传入 LLM 上下文
5. **推荐结果持久化**：将推荐结果保存到数据库，支持历史查看

### P2 — 可以延后
6. **推荐结果分享**：生成分享链接或导出 PDF
7. **A/B 测试不同 Prompt 策略**

## 功能边界（明确不做的事）
- 不做实时数据大屏/仪表盘（那是蓝海雷达的职责）
- 不做自动内容生成（那是 AI Script 的职责）
- 不做竞品频道深度分析（那是频道分析的职责）
- 多轮对话不做独立聊天界面，而是在推荐结果下方以追问形式展开

## 技术约束与交付形式
- 前端：React 18 + Ant Design + Tailwind CSS（现有技术栈）
- 后端：FastAPI + Pydantic + 火山引擎 LLM（现有技术栈）
- 多轮对话复用现有 `llm_conversation_service.py` 的对话记忆机制
- 交付形式：直接修改现有文件，不新建页面

## 验收标准

### AC-1：表单增强
- 新增 `existing_channel_url`、`target_regions`、`weekly_hours` 三个字段
- 所有必填项有校验提示

### AC-2：AI 推荐深度提升
- 返回 3 个推荐（含1个高增长潜力品类）
- 每个推荐包含 `action_roadmap`（30天行动路线图）和 `estimated_monthly_income`
- JSON 解析容错：格式错误时重试最多3次，全部失败有友好提示
- 多轮对话：用户追问后 AI 基于上下文回答，对话历史持久化

### AC-3：UI 视觉升级
- 推荐卡片：匹配度用进度条、星级用图标、行动路线图用 Timeline
- 避坑卡片：精致警告色调
- 整体符合现代SaaS极简风：留白、阴影、字体层级

### AC-4：功能完整性
- 「一键导入蓝海雷达」按钮功能正常
- 已有频道 URL 传入后端，AI 能基于频道信息推荐
- 配额检查和消耗明细正常显示

## 风险识别
1. **LLM 输出 JSON 格式不稳定** → 已有重试+容错机制，需扩展校验新字段
2. **多轮对话上下文过长** → 复用现有 auto-truncation 机制（DEFAULT_MAX_CHARS=8000）
3. **YouTube API 配额消耗** → 已有配额前置检查，新增频道分析会增加1次 channels.list 调用
4. **新增字段导致 Prompt 过长** → 控制单次 Prompt 在 2000 字以内
