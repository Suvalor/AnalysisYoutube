# 项目需求文档（PRD）

## 项目概述

YouTube Compass 四项功能增强：出海导航联动频道管理、竞对洞察与视频看板 AI 增强、README 中英双版、蓝海雷达 AI 参数全自动闭环迭代。

## 目标用户与使用场景

- **目标用户**：YouTube 出海内容创作者 / 运营团队
- **使用场景**：
  1. 用户在出海导航看到推荐的头部博主，希望一键关注入库，无需手动复制链接
  2. 用户在竞对洞察页面希望获得 AI 生成的竞争格局摘要，而非仅看折线图
  3. 用户在视频看板希望获得 AI 内容策略建议
  4. 用户希望蓝海雷达参数能自动迭代优化，无需每次手动复盘
  5. 开发者/用户需要中英双版 README 了解项目

## 核心功能列表

### P0 — 必须完成

1. **出海导航一键关注**：NavigationGuide 的 ChannelBreakdownCard 增加「关注」按钮，点击后调用 analyzeYouTubeBatchApi 入库，用户留在导航页面继续查看
2. **蓝海雷达 AI 参数全自动闭环**：
   - 复盘推荐参数自动持久化到数据库（radar_param_iterations 表）
   - 下次扫描自动回填最新推荐参数作为默认值
   - 迭代效果追踪（对比前后参数下的扫描结果差异）
   - 定时自动触发复盘（可配置周期，默认每日）
3. **README.md 中英双版**：拆分为 README.md（中文）+ README_EN.md（英文），两文件顶部互相链接

### P1 — 重要

4. **竞对洞察 AI 洞察**：增加 LLM 生成竞争格局摘要（对比频道的定位差异、内容策略差异、受众重叠度），支持选择模型/智能体
5. **视频看板 AI 建议**：增加 LLM 生成内容策略建议（基于看板中视频项目状态和关联剧本数据）

### P2 — 锦上添花

6. **迭代效果可视化**：蓝海雷达参数迭代历史图表（参数变化趋势 + 扫描效果变化趋势）

## 功能边界（明确不做的事）

- 不做出海导航的批量关注（一次只关注一个频道）
- 不做竞对洞察的实时数据推送（仍为手动触发）
- 不做视频看板的自动化发布流程
- 不做蓝海雷达的跨用户参数共享
- 不做多语言 i18n 框架（仅 README 双版）

## 技术约束与交付形式

- **技术栈**：沿用项目现有架构（FastAPI + React + TypeScript + Ant Design + Zustand + SQLAlchemy）
- **LLM 调用**：复用现有 LLMClientFactory + 配置中心模型/智能体体系
- **定时任务**：使用 APScheduler（FastAPI 集成）实现定时复盘
- **交付形式**：前后端代码 + 数据库迁移 + 更新的 README 文件

## 验收标准

### AC-1：出海导航一键关注
- NavigationGuide 的 ChannelBreakdownCard 显示「关注」按钮
- 点击关注后调用 analyzeYouTubeBatchApi 入库，按钮变为「已关注」
- 已入库频道按钮置灰，不重复入库
- 关注操作不离开当前页面

### AC-2：蓝海雷达 AI 参数全自动闭环
- 复盘推荐参数保存到 radar_param_iterations 表
- 扫描页面自动加载最新推荐参数作为默认值
- 迭代记录包含：参数快照、扫描结果摘要、迭代时间
- 定时复盘可通过 API 手动触发，也可按配置周期自动执行
- 迭代效果可查询（API 返回历史迭代记录）

### AC-3：README 中英双版
- README.md 为中文版，README_EN.md 为英文版
- 两份文件顶部互相链接，可一键切换语言
- 内容覆盖：项目概述、功能列表、快速开始、技术栈、Feature Flags

### AC-4：竞对洞察 AI 洞察
- 竞对洞察页面增加「AI 竞争分析」按钮
- 点击后调用 LLM 生成竞争格局摘要
- 支持选择模型配置和智能体
- 摘要包含：定位差异、内容策略差异、受众重叠度分析

### AC-5：视频看板 AI 建议
- 视频看板页面增加「AI 策略建议」区域
- 基于看板中视频项目状态生成内容策略建议
- 支持选择模型配置和智能体

## 风险识别（初步）

1. **APScheduler 依赖**：项目当前无定时任务框架，需引入 APScheduler 并确保与 FastAPI 生命周期集成
2. **LLM 调用成本**：定时复盘会周期性调用 LLM，需考虑成本控制（可配置开关和频率）
3. **参数回填冲突**：自动回填参数可能与用户手动调整冲突，需明确优先级（用户手动 > 自动推荐）
4. **YouTube API 配额**：定时复盘触发的扫描会消耗配额，需与现有配额追踪集成
