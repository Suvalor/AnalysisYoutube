# 项目需求文档（PRD）— 完善登录注册功能

## 项目概述
完善现有认证系统，增加：登录图形验证码+记住账号优化、注册流程（手机号+邮箱绑定+验证码）、忘记密码（邮箱重置链接）。

## 目标用户与使用场景
- **目标用户**：YouTube Compass 平台的用户
- **使用场景**：用户注册时用手机号作为用户名，绑定邮箱验证；登录时需图形验证码防刷；忘记密码通过邮箱重置链接找回

## 核心功能列表

### P0 — 必须完成
1. **登录增强**
   - 图形验证码：后端生成随机字符串+噪点图片，登录时需输入验证码
   - 记住账号：当前已实现 localStorage 存储邮箱+密码，需改为仅存邮箱（安全性）
2. **注册流程重构**
   - 用户名改为手机号（User model 新增 phone 字段，必填唯一）
   - 必须绑定邮箱：注册时先填手机号+密码，再绑定邮箱
   - 邮箱验证码：发送6位数字验证码到邮箱，用户输入验证
   - 验证链接地址：查询当前请求域名，拼接重置/验证路径
   - 邮箱验证通过后才算注册完成（is_active=True）
3. **忘记密码**
   - 用户输入手机号→查找关联邮箱→发送重置链接到邮箱
   - 重置链接含 JWT token，有效期30分钟
   - 点击链接→前端展示重置密码页面→设置新密码

### P1 — 应该完成
4. 验证码频率限制（同一邮箱60秒内只能发一次）
5. 图形验证码有效期5分钟，用过即废

## 功能边界（明确不做的事）
- 不做手机短信验证码（需短信服务商，成本高）
- 不做 OAuth 第三方登录增强
- 不做二次验证/2FA
- 不修改现有 JWT 认证机制的核心逻辑

## 技术约束与交付形式
- 前端：React 18 + Ant Design（现有技术栈）
- 后端：FastAPI + Pydantic + SQLAlchemy（现有技术栈）
- 图形验证码：Python Pillow 生成
- 邮件发送：复用现有邮件服务（SMTP），在 config.py 新增 SMTP 配置项
- 交付形式：修改现有文件 + 新增必要文件

## 验收标准

### AC-1：图形验证码
- 登录页面显示图形验证码图片
- 点击图片可刷新验证码
- 验证码输入错误时提示"验证码错误"
- 验证码5分钟过期

### AC-2：记住账号
- 勾选记住账号后，下次登录自动填入邮箱（不存密码）
- 不勾选时，清除已存邮箱

### AC-3：注册流程
- 注册表单：手机号（必填）+ 密码 + 确认密码 + 邮箱 + 邮箱验证码
- 发送验证码按钮，60秒倒计时
- 验证码验证通过后注册成功
- User model 新增 phone 字段

### AC-4：忘记密码
- 登录页有"忘记密码"链接
- 输入手机号→发送重置链接到邮箱
- 点击链接→重置密码页面→设置新密码→登录

## 风险识别
1. **邮件服务不可用** → 需检查 SMTP 配置，失败时友好提示
2. **验证码被暴力破解** → 图形验证码+频率限制双重防护
3. **重置链接泄露** → JWT 短有效期(30min)+一次性使用
4. **手机号格式校验** → 需支持中国大陆手机号格式
