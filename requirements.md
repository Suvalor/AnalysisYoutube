# 项目需求文档（PRD）— 国际化系统

## 项目概述
为 YouTube Compass 新增国际化（i18n）系统，支持中文、英文、日文、韩文四种语言。用户可在个人设置页切换语言，语言偏好持久化到后端实现跨设备同步。全量翻译覆盖 UI 文本、后端消息和用户生成内容。

## 目标用户与使用场景
- **目标用户**：YouTube Compass 的所有注册用户（出海创作者，覆盖中/英/日/韩市场）
- **使用场景**：非中文用户希望以母语使用工具；中文用户在海外协作时需要英文界面；日韩市场用户需要本地化体验

## 核心功能列表

### P0（必须交付）
1. **i18n 基础设施**：react-i18next 集成 + i18next 初始化 + 语言检测
2. **4 种语言翻译文件**：zh-CN（中文）、en-US（英文）、ja-JP（日文）、ko-KR（韩文）
3. **核心 UI 翻译**：导航栏、TabbedShell、登录/注册、设置中心、个人设置等核心组件
4. **语言切换 UI**：个人设置页增加语言切换选项
5. **语言持久化**：后端 API 存储 locale 偏好，登录后自动恢复

### P1（优先交付）
6. **业务页面翻译**：蓝海雷达、竞对洞察、频道管理、视频看板、AI 脚本工坊、SOP 工作流等
7. **Ant Design locale 同步**：切换语言时同步 Ant Design 组件库的 locale
8. **后端错误消息翻译**：API 返回的错误消息支持多语言

### P2（后续优化）
9. **用户内容自动翻译**：用户生成的脚本/笔记等内容通过 LLM 自动翻译
10. **语言检测**：根据浏览器 navigator.language 自动检测首选语言

## 功能边界（明确不做的事）
- 不做翻译管理后台（使用 JSON 文件 + Git 管理）
- 不做实时翻译协作平台
- 不做 RTL（从右到左）布局支持（日韩均为 LTR）
- 不做翻译缓存服务端（纯前端 JSON 加载）

## 技术约束与交付形式
- **前端**：react-i18next + i18next + i18next-browser-languagedetector
- **翻译文件**：JSON 格式，按 namespace 分文件（common/nav/auth/settings/radar/youtube/ai/sop）
- **后端**：User model 新增 locale 字段，API 错误消息支持 Accept-Language 头
- **持久化**：后端 user_settings 新增 locale 字段，前端 localStorage 做即时缓存

## 验收标准
1. 个人设置页可看到 4 种语言选项，点击即可切换
2. 切换语言后全站 UI 文本即时切换（导航、按钮、表单标签、提示消息等）
3. Ant Design 组件（日期选择器、分页、表单验证等）同步切换语言
4. 刷新页面后语言保持不变
5. 换设备登录后语言自动同步
6. 所有 4 种语言的翻译文件完整，无遗漏 key

## Sprint 规划建议
- **Sprint 1**（i18n 基础设施 + 核心翻译）：react-i18next 集成 + 语言 Store + 翻译文件结构 + 核心组件翻译（TabbedShell/登录注册/设置中心/个人设置）+ Ant Design locale 同步
- **Sprint 2**（业务页面 + 后端集成）：业务页面翻译提取 + 后端 locale API + 个人设置语言切换 + 后端错误消息翻译

## 风险识别
1. **翻译工作量**：1306 处中文硬编码，提取和翻译工作量巨大
2. **翻译质量**：日韩翻译需要专业校对，AI 翻译可能有误差
3. **动态内容**：部分文本包含变量插值（如 `已保存 ${count} 个`），需统一使用 i18n 插值
4. **Ant Design locale 覆盖**：部分 Ant Design 组件的 locale 可能不完整
