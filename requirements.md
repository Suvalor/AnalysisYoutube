# 项目需求文档（PRD）— 多主题系统

## 项目概述
为 YouTube Compass 新增多主题系统，参考 liblib.art 等 AI 创作平台的视觉风格，支持 3-4 个主题切换。用户可通过头像下拉菜单进入个人设置切换主题，主题偏好持久化到后端实现跨设备同步。

## 目标用户与使用场景
- **目标用户**：YouTube Compass 的所有注册用户
- **使用场景**：用户希望根据个人偏好或工作环境（白天/夜间）切换不同的视觉主题；在不同设备上登录时自动同步主题偏好

## 核心功能列表

### P0（必须交付）
1. **主题基础设施**：基于 CSS 变量的主题系统，支持运行时动态切换
2. **4 个主题定义**：
   - **浅色经典**（Light Classic）：当前默认风格，slate 灰色系 + 蓝色强调
   - **LibLib 暗色**（LibLib Dark）：参考 liblib.art，深色背景 + 紫蓝渐变 + 毛玻璃效果
   - **深蓝科技**（Deep Blue）：深蓝背景 + 青色/蓝色强调，适合数据密集场景
   - **暖橙活力**（Warm Orange）：浅暖色背景 + 橙色/珊瑚色强调，温暖友好
3. **主题切换 UI**：头像下拉菜单 → 个人设置 → 主题选择
4. **主题持久化**：后端 API 存储用户主题偏好，登录后自动恢复

### P1（优先交付）
5. **Ant Design 主题适配**：通过 ConfigProvider 的 theme 属性同步 Ant Design 组件主题
6. **全局组件适配**：TabbedShell、标签栏、侧边栏、设置中心等核心组件适配所有主题

### P2（后续优化）
7. **主题预览**：切换前可预览主题效果
8. **自定义主题**：用户可微调主题色

## 功能边界（明确不做的事）
- 不做主题编辑器/自定义主题色功能
- 不做主题市场/社区分享
- 不做跟随系统（prefers-color-scheme）自动切换（P2 再考虑）
- 不做主题过渡动画（保持即时切换）

## 技术约束与交付形式
- **前端**：React 18 + Tailwind CSS + Ant Design 5 + Zustand
- **后端**：FastAPI + SQLAlchemy + MySQL
- **主题方案**：CSS 变量 + Ant Design ConfigProvider theme + Zustand store
- **持久化**：后端 user_settings 表新增 theme 字段，前端 localStorage 做即时缓存

## 验收标准
1. 用户可在头像下拉菜单点击「个人设置」进入设置页
2. 设置页可看到 4 个主题选项，点击即可切换
3. 切换后全站（侧边栏、标签栏、内容区、Ant Design 组件）即时生效
4. 刷新页面后主题保持不变
5. 换设备登录后主题自动同步
6. 每个主题的视觉风格与设计稿一致

## Sprint 规划建议
- **Sprint 1**（主题基础设施）：CSS 变量体系 + 4 个主题定义 + useThemeStore + Tailwind 适配 + Ant Design 主题同步
- **Sprint 2**（UI 入口与后端集成）：头像下拉菜单 + 个人设置页 + 后端 theme API + 全局组件适配

## 风险识别
1. **Tailwind 硬编码颜色**：现有代码大量使用 `bg-slate-*`、`text-slate-*` 等硬编码颜色，需逐步替换为 CSS 变量
2. **Ant Design 主题覆盖范围**：部分 Ant Design 组件的深色模式可能需要额外调整
3. **性能**：CSS 变量切换是运行时操作，需确保无闪烁
